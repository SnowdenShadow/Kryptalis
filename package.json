{
  "name": "dockcontrol",
  "version": "0.1.0",
  "private": true,
  "description": "The next-generation self-hosted infrastructure platform",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "dev:api": "pnpm --filter @dockcontrol/api dev",
    "dev:dashboard": "pnpm --filter @dockcontrol/dashboard dev",
    "build:api": "pnpm --filter @dockcontrol/api build",
    "build:dashboard": "pnpm --filter @dockcontrol/dashboard build",
    "db:generate": "pnpm --filter @dockcontrol/api db:generate",
    "db:migrate": "pnpm --filter @dockcontrol/api db:migrate",
    "db:push": "pnpm --filter @dockcontrol/api db:push",
    "db:studio": "pnpm --filter @dockcontrol/api db:studio",
    "docker:up": "docker compose -f docker/docker-compose.dev.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.dev.yml down",
    "test": "turbo test",
    "clean": "turbo clean && node -e \"fs.rmSync('node_modules',{recursive:true,force:true})\""
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^2.3.3"
  }
}
