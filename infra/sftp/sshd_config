# Kryptalis SFTP daemon config.
#
# Two principles:
#   1. SFTP-only — every non-root login is forced to internal-sftp.
#      No shell, no port forward, no agent forward, no X11.
#   2. Chrooted — every account's home dir is bind-mounted onto its
#      Kryptalis app sandbox at create time; ChrootDirectory %h locks
#      the user into that home.

Port 22
AddressFamily any
ListenAddress 0.0.0.0
ListenAddress ::

# Host keys generated at first boot by the entrypoint.
HostKey /etc/ssh/ssh_host_ed25519_key
HostKey /etc/ssh/ssh_host_rsa_key

# Logging — every login attempt visible in `docker logs`.
SyslogFacility AUTH
LogLevel VERBOSE

# Authentication
PermitRootLogin no
PasswordAuthentication yes
PubkeyAuthentication yes
PermitEmptyPasswords no
ChallengeResponseAuthentication no
KbdInteractiveAuthentication no
UsePAM yes
# Brute-force defense: allow 3 auth failures per connection before
# tarpitting. MaxStartups raised because Filezilla aggressively
# parallelizes operations (it opens up to 10 connections at once,
# which the default 10:30:60 cap would refuse with ECONNREFUSED).
# 100 simultaneous unauthenticated still cuts off real botnets.
MaxAuthTries 3
MaxStartups 100:30:200
# Drop idle sessions after 5 minutes so FileZilla forgetting to
# disconnect doesn't keep slots tied up.
ClientAliveInterval 60
ClientAliveCountMax 5

# Force SFTP-only for everyone except root (which is locked anyway).
Subsystem sftp internal-sftp -l VERBOSE

# Chroot + sftp lock for every real account. The API matches against
# the sftpusers group it adds every user to. Per-user ChrootDirectory
# overrides are dropped into /etc/ssh/sshd_config.d/<user>.conf by the
# Kryptalis API so each user lands directly in their app sandbox
# without any runtime mount --bind on Docker-managed volumes (which
# can race the Docker daemon and corrupt the volume).
Match Group sftpusers
  ChrootDirectory %h
  ForceCommand internal-sftp -l VERBOSE
  AllowTcpForwarding no
  X11Forwarding no
  AllowAgentForwarding no
  PermitTunnel no
  PermitTTY no

# Per-user includes — the API drops one .conf file per account here so
# each can have its own ChrootDirectory pointing straight at the right
# host path. Loaded AFTER the Match block above; later directives win.
Include /etc/ssh/sshd_config.d/*.conf
